<nav class="navbar navbar-expand-lg navbar-light mb-5">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <span class="me-3">🛍</span> Good Market
        </a>
        <ul class="navbar-nav d-flex flex-row">
            <% if (request.user) { %>
                <%- await include("./navLink", {request, url: "/post/create", content: "Create post", activeContent: null}) %>
                <%- await include("./navLink", {
                    request,
                    url: "/message/",
                    content: "<i class='bi bi-chat-right-text'></i>",
                    activeContent: "<i class='bi bi-chat-right-text-fill'></i>"
                }) %>
                <%- await include("./userNavOptions", {request}) %>
            <% } else { %>
                <%- await include("./navLink", {request, url: "/login", content: "Login", activeContent: null}) %>
                <%- await include("./navLink", {request, url: "/register", content: "Register", activeContent: null}) %>
            <% } %>
        </ul>
    </div>

</nav>
